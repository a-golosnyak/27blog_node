class Comment {
  static messages () {
    return {
      "required.body": "Parameter :attribute required.",
      "required.userId": "Parameter :attribute required.",
      "required.postId": "Parameter :attribute required.",
      "string.body": "Parameter :attribute must be string.",
      "string.userId": "Parameter :attribute must be string.",
      "string.postId": "Parameter :attribute must be string."
    }
  }
}

export default Comment
